rule_type = "histogram"
metric_name = "rox_sensor_scan_call_duration_milliseconds"
display_name = "rox_sensor_scan_call_duration_milliseconds"
description = "Scan call duration"
reviewed = "No, AI-generated"
last_review_by = ""
last_review_on = "never"

[histogram_config]
unit = "milliseconds"

[thresholds]
p95_good = 500
p95_warn = 2000

[messages]
green = "p95={p95:.0f}ms, p99={p99:.0f}ms (good)"
yellow = "p95={p95:.0f}ms, p99={p99:.0f}ms (elevated)"
red = "p95={p95:.0f}ms, p99={p99:.0f}ms (high latency)"

[remediation]
red = "High scan call latency. Check scanner performance and connectivity."
yellow = "Monitor scan call latency trends."

acs_versions = ["4.7+", "4.8+", "4.9+"]

