rule_type = "gauge_threshold"
metric_name = "rox_sensor_network_flow_buffer_size"
display_name = "rox_sensor_network_flow_buffer_size"
description = "Network flow buffer size monitoring"
reviewed = "No, AI-generated"
last_review_by = ""
last_review_on = "never"

[thresholds]
low = 100
high = 200
higher_is_worse = true

[load_level_thresholds.low]
low = 100
high = 150

[load_level_thresholds.medium]
low = 150
high = 250

[load_level_thresholds.high]
low = 200
high = 300

[messages]
green = "Buffer size: {value} (default: 100, max: 300)"
yellow = "Buffer size: {value} - elevated"
red = "Buffer size: {value} - at/near capacity"

[remediation]
red = "Increase buffer size via sensor configuration. Check network flow volume and upstream processing bottlenecks."
yellow = "Monitor buffer size trends. If consistently elevated, consider increasing buffer capacity."

acs_versions = ["4.7+", "4.8+", "4.9+"]

